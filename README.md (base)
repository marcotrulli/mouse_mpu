# Mouse MPU6050 via TCP

Sistema Raspberry Pi + MPU6050 che muove il mouse del PC via TCP.

---

## Setup Pi

```bash
git clone <repo>
cd mouse_mpu_tcp
python3 -m venv venv --system-site-packages
source venv/bin/activate
pip install --upgrade pip
pip install mpu6050-raspberrypi
python3 mouse_pi.py
